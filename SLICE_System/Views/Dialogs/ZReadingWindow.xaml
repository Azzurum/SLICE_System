<Window x:Class="SLICE_System.Views.Dialogs.ZReadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="End of Shift - Z-Reading" Height="500" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#1E2B3C" FontFamily="Segoe UI" WindowStyle="ToolWindow">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30,20">
        <StackPanel VerticalAlignment="Center">

            <fa:ImageAwesome Icon="Lock" Height="35" Foreground="#BDC3C7" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="END OF SHIFT" FontSize="26" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="Cash Drawer Reconciliation" FontSize="13" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,0,0,25"/>

            <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,25">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#000000" ShadowDepth="4" Opacity="0.15"/>
                </Border.Effect>
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                        <fa:ImageAwesome Icon="InfoCircle" Height="12" Foreground="#7F8C8D" Margin="0,0,6,0"/>
                        <TextBlock Text="SYSTEM EXPECTED CASH" FontSize="11" FontWeight="Bold" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock x:Name="txtExpectedCash" Text="₱0.00" FontSize="34" FontWeight="Black" Foreground="#27AE60" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Enter Physical Cash in Drawer:" FontSize="13" FontWeight="SemiBold" Foreground="#BDC3C7" Margin="0,0,0,8"/>

            <Border Background="#2C3E50" CornerRadius="8" Height="60">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" Color="#000000" ShadowDepth="1" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="₱" FontSize="24" FontWeight="Black" Foreground="#7F8C8D" VerticalAlignment="Center" Margin="20,0,5,0"/>

                    <TextBox x:Name="txtActualCash" Grid.Column="1" Background="Transparent" BorderThickness="0" 
                             Foreground="White" FontSize="28" FontWeight="Bold" VerticalContentAlignment="Center" 
                             CaretBrush="White" Padding="0,0,20,0"/>
                </Grid>
            </Border>

            <Button Content="SUBMIT &amp; LOGOUT" Background="#E74C3C" Style="{StaticResource ModernButtonStyle}" Height="50" Margin="0,35,0,10" Click="Submit_Click"/>

            <Button Content="Cancel / Return to POS" Background="Transparent" Foreground="#7F8C8D" FontSize="12" Style="{StaticResource ModernButtonStyle}" Height="30" Click="Cancel_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="TextDecorations" Value="Underline"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </StackPanel>
    </Grid>
</Window>