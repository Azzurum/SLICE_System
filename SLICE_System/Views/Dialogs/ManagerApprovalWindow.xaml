<Window x:Class="SLICE_System.Views.Dialogs.ManagerApprovalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Manager Authorization Required" Height="500" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        WindowStyle="None" AllowsTransparency="True" Background="#90000000" 
        FontFamily="Segoe UI" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style x:Key="ModernBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" Width="360" Height="420" CornerRadius="16" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="#000000" ShadowDepth="10" Opacity="0.25"/>
            </Border.Effect>

            <Grid Margin="35,30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <Border Background="#FDEDEC" Width="64" Height="64" CornerRadius="32" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <fa:ImageAwesome Icon="Lock" Height="28" Foreground="#E74C3C" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="MANAGER OVERRIDE" FontSize="22" FontWeight="Black" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                    <TextBlock Text="Requires Manager/Admin credentials." FontSize="13" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,0,20">
                    <TextBlock Text="USERNAME" FontSize="11" FontWeight="Bold" Foreground="#7F8C8D" Margin="0,0,0,6"/>
                    <Border Background="#F8F9FA" BorderBrush="#EAECEE" BorderThickness="1.5" CornerRadius="8" Height="45">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="45"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <fa:ImageAwesome Icon="User" Height="16" Foreground="#BDC3C7" HorizontalAlignment="Center"/>
                            <TextBox x:Name="txtUsername" Grid.Column="1" Background="Transparent" BorderThickness="0" 
                                     FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50" VerticalContentAlignment="Center" Padding="5,0,15,0"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,30">
                    <TextBlock Text="PASSWORD" FontSize="11" FontWeight="Bold" Foreground="#7F8C8D" Margin="0,0,0,6"/>
                    <Border Background="#F8F9FA" BorderBrush="#EAECEE" BorderThickness="1.5" CornerRadius="8" Height="45">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="45"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <fa:ImageAwesome Icon="Key" Height="16" Foreground="#BDC3C7" HorizontalAlignment="Center"/>
                            <PasswordBox x:Name="txtPassword" Grid.Column="1" Background="Transparent" BorderThickness="0" 
                                         FontSize="16" Foreground="#2C3E50" VerticalContentAlignment="Center" Padding="5,0,15,0" 
                                         KeyDown="txtPassword_KeyDown"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="3" VerticalAlignment="Bottom">
                    <Button Content="AUTHORIZE OVERRIDE" Style="{StaticResource ModernBtn}" Background="#E74C3C" Height="50" Click="Authorize_Click" Margin="0,0,0,15">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="15" Color="#E74C3C" ShadowDepth="4" Opacity="0.3"/>
                        </Button.Effect>
                    </Button>

                    <Button Content="Cancel / Go Back" Background="Transparent" Foreground="#95A5A6" FontSize="13" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="Cancel_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="#2C3E50"/>
                                                    <Setter Property="TextDecorations" Value="Underline"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>