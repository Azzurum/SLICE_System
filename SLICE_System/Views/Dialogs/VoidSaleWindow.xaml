<Window x:Class="SLICE_System.Views.Dialogs.VoidSaleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Transaction History &amp; Voids" Height="650" Width="900"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        WindowStyle="None" AllowsTransparency="True" Background="#90000000" 
        FontFamily="Segoe UI" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style x:Key="ModernBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" Width="800" Height="550" CornerRadius="16" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="#000000" ShadowDepth="10" Opacity="0.25"/>
            </Border.Effect>

            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Close_Click" Margin="0,-10,-10,0" ToolTip="Close">
                    <fa:ImageAwesome Icon="Times" Height="16" Foreground="#BDC3C7"/>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.6"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#FDEDEC" Width="48" Height="48" CornerRadius="24" Margin="0,0,15,0">
                            <fa:ImageAwesome Icon="History" Height="20" Foreground="#E74C3C" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="TRANSACTION HISTORY" FontSize="22" FontWeight="Black" Foreground="#2C3E50"/>
                            <TextBlock Text="Select a completed sale from today to void and refund." FontSize="13" Foreground="#95A5A6"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="1" BorderBrush="#EAECEE" BorderThickness="1" CornerRadius="8" Background="#FAFCFC" Padding="10">
                    <DataGrid x:Name="dgSales" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" 
                              Background="Transparent" BorderThickness="0" RowHeight="50" VerticalScrollBarVisibility="Auto"
                              GridLinesVisibility="None">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#7F8C8D"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="10,0,10,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#EAECEE"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="#F4F6F6"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F3F4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="10,0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#FDEDEC"/>
                                        <Setter Property="Foreground" Value="#C0392B"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="REF ID" Binding="{Binding ReferenceNumber}" Width="110" FontWeight="Bold"/>
                            <DataGridTextColumn Header="TIME" Binding="{Binding LocalTransactionDate, StringFormat='hh:mm tt'}" Width="90" Foreground="#95A5A6"/>
                            <DataGridTextColumn Header="PRODUCT ITEM" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="QTY" Binding="{Binding QuantitySold}" Width="60" FontWeight="Black"/>
                            <DataGridTextColumn Header="AMOUNT" Binding="{Binding TotalAmount, StringFormat='₱{0:N2}'}" Width="100" FontWeight="Bold" Foreground="#27AE60"/>
                            <DataGridTextColumn Header="METHOD" Binding="{Binding PaymentMethod}" Width="90" Foreground="#7F8C8D"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="220"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="REASON FOR VOID" FontSize="11" FontWeight="Bold" Foreground="#7F8C8D" Margin="0,0,0,6"/>
                        <Border Background="#F8F9FA" BorderBrush="#EAECEE" BorderThickness="1.5" CornerRadius="8" Height="45">
                            <TextBox x:Name="txtReason" Background="Transparent" BorderThickness="0" FontSize="14" Foreground="#2C3E50" VerticalContentAlignment="Center" Padding="15,0" Tag="e.g. Customer changed mind, Error in order..."/>
                        </Border>
                    </StackPanel>

                    <Button Grid.Column="1" Content="VOID TRANSACTION" Style="{StaticResource ModernBtn}" Background="#E74C3C" Height="45" VerticalAlignment="Bottom" Click="Void_Click">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" Color="#E74C3C" ShadowDepth="2" Opacity="0.3"/>
                        </Button.Effect>
                    </Button>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</Window>