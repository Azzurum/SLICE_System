<UserControl x:Class="SLICE_System.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Background="#F4F6F7" FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="TextDark" Color="#2C3E50"/>
        <SolidColorBrush x:Key="TextLight" Color="#7F8C8D"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Color="#BDC3C7" ShadowDepth="2" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="350"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Command Center" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextDark}"/>
                    <TextBlock Text="{Binding DashboardTitle}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextLight}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox x:Name="cmbBranches" Width="150" Height="35" Margin="0,0,10,0" Padding="10,5" DisplayMemberPath="BranchName" SelectedValuePath="BranchID" SelectionChanged="cmbBranches_SelectionChanged" Background="White" BorderBrush="#EAECEE"/>
                    <ComboBox x:Name="cmbDateRange" Width="130" Height="35" Margin="0,0,10,0" Padding="10,5" SelectionChanged="cmbDateRange_SelectionChanged" Background="White" BorderBrush="#EAECEE">
                        <ComboBoxItem Content="Today" IsSelected="True"/>
                        <ComboBoxItem Content="Yesterday"/>
                        <ComboBoxItem Content="Last 7 Days"/>
                        <ComboBoxItem Content="Last 30 Days"/>
                        <ComboBoxItem Content="Custom Range"/>
                    </ComboBox>
                    <StackPanel x:Name="pnlCustomDate" Orientation="Horizontal" Visibility="Collapsed" Margin="0,0,10,0">
                        <DatePicker x:Name="dpStart" Width="110" Height="35"/>
                        <TextBlock Text="-" VerticalAlignment="Center" Margin="5,0"/>
                        <DatePicker x:Name="dpEnd" Width="110" Height="35"/>
                    </StackPanel>
                    <Button Click="Refresh_Click" Width="35" Height="35" Background="#2C3E50" Foreground="White" BorderThickness="0" Cursor="Hand" ToolTip="Refresh Data">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                        <fa:ImageAwesome Icon="Refresh" Height="14" Foreground="White"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,15,0" Padding="20">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="GROSS REVENUE" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                            <TextBlock Text="{Binding Metrics.TotalRevenue, StringFormat='₱{0:N2}'}" FontSize="24" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding Metrics.TotalSalesCount, StringFormat='{}{0:N0} Transactions'}" FontSize="12" Foreground="{StaticResource TextLight}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border Background="#E8F8F5" Width="45" Height="45" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <fa:ImageAwesome Icon="LineChart" Height="20" Foreground="#27AE60"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0,0,15,0" Padding="20" Visibility="{Binding AdminOnlyVisibility}">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="NET PROFIT" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                            <TextBlock Text="{Binding Metrics.NetProfit, StringFormat='₱{0:N2}'}" FontSize="24" FontWeight="Black" Foreground="#2980B9" Margin="0,5,0,0"/>
                            <TextBlock Text="Revenue minus all expenses" FontSize="12" Foreground="{StaticResource TextLight}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border Background="#EAF2F8" Width="45" Height="45" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <fa:ImageAwesome Icon="Bank" Height="20" Foreground="#2980B9"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="0,0,15,0" Padding="20">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="WASTE &amp; LEAKAGE" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                            <TextBlock Text="{Binding Metrics.TotalWasteCost, StringFormat='-₱{0:N2}'}" FontSize="24" FontWeight="Black" Foreground="#C0392B" Margin="0,5,0,0"/>
                            <TextBlock Text="Cost of discarded/missing stock" FontSize="12" Foreground="{StaticResource TextLight}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border Background="#FDEDEC" Width="45" Height="45" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <fa:ImageAwesome Icon="Trash" Height="20" Foreground="#C0392B"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Column="3" Style="{StaticResource CardStyle}" Padding="20">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="URGENT ALERTS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                            <TextBlock Text="{Binding Metrics.LowStockCount}" FontSize="24" FontWeight="Black" Foreground="#E67E22" Margin="0,5,0,0"/>
                            <TextBlock Text="Items requiring restock" FontSize="12" Foreground="{StaticResource TextLight}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border Background="#FEF5E7" Width="45" Height="45" CornerRadius="8" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <fa:ImageAwesome Icon="Warning" Height="20" Foreground="#E67E22"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,20,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Performance vs Expenses" FontSize="16" FontWeight="Black" Foreground="{StaticResource TextDark}"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding BranchSeries}" LegendLocation="Top" Margin="0,15,0,0">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding BranchLabels}" Foreground="{StaticResource TextLight}">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator StrokeThickness="0"/>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis LabelFormatter="{Binding CurrencyFormatter}" Foreground="{StaticResource TextLight}">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Stroke="#EAECEE"/>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Top Products" FontSize="16" FontWeight="Black" Foreground="{StaticResource TextDark}"/>
                        <lvc:PieChart Grid.Row="1" Series="{Binding ProductSeries}" LegendLocation="Bottom" InnerRadius="40" Margin="0,10,0,0">
                            <lvc:PieChart.ChartLegend>
                                <lvc:DefaultLegend BulletSize="10" Foreground="{StaticResource TextDark}"/>
                            </lvc:PieChart.ChartLegend>
                        </lvc:PieChart>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,20,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Low Stock Alerts" FontSize="16" FontWeight="Black" Foreground="#E67E22" Margin="0,0,0,15"/>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Metrics.Alerts}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="None" Background="Transparent" BorderThickness="0" RowHeight="45" GridLinesVisibility="None">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding BranchName}" Width="*" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                                <DataGridTextColumn Binding="{Binding ItemName}" Width="*" Foreground="{StaticResource TextLight}"/>
                                <DataGridTemplateColumn Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="#FDEDEC" CornerRadius="4" Padding="5,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock Text="CRITICAL" Foreground="#C0392B" FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Recent Transactions" FontSize="16" FontWeight="Black" Foreground="{StaticResource TextDark}" Margin="0,0,0,15"/>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Metrics.RecentActivity}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="40" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EAECEE">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#EAECEE"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="5"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding TimeAgo}" Width="100" Foreground="{StaticResource TextLight}"/>
                                <DataGridTextColumn Header="Branch" Binding="{Binding BranchName}" Width="*" FontWeight="Bold" Foreground="{StaticResource TextDark}" Visibility="{Binding DataContext.AdminOnlyVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="1.5*"/>
                                <DataGridTextColumn Header="Amount" Binding="{Binding TotalAmount, StringFormat=₱{0:N2}}" Width="100" FontWeight="Black" Foreground="#27AE60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>