<UserControl x:Class="SLICE_System.Views.ManageRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             Background="#F5F7FA" FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryText" Color="#2C3E50"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#7F8C8D"/>
        <SolidColorBrush x:Key="AccentColor" Color="#E67E22"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#2ECC71"/>
        <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" Padding="15,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="5" ShadowDepth="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" CenterX="100" CenterY="25"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Margin="0,0,0,25">
                <TextBlock Text="Logistics Console" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryText}"/>
                <TextBlock Text="Review and process incoming stock requests." FontSize="14" Foreground="{StaticResource SecondaryText}" Margin="0,5,0,0"/>
            </StackPanel>

            <Grid x:Name="EmptyStateOverlay" Grid.Row="1" Visibility="Collapsed" Panel.ZIndex="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5">
                    <fa:ImageAwesome Icon="Inbox" Height="60" Foreground="#BDC3C7" Margin="0,0,0,15"/>
                    <TextBlock Text="All Caught Up!" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource SecondaryText}" HorizontalAlignment="Center"/>
                    <TextBlock Text="No pending requests at the moment." FontSize="14" Foreground="{StaticResource SecondaryText}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="5">
                <DataGrid x:Name="dgRequests" AutoGenerateColumns="False" IsReadOnly="True" 
                          SelectionMode="Single" SelectionChanged="dgRequests_SelectionChanged"
                          Background="White" BorderThickness="0" RowHeight="55"
                          HeadersVisibility="Column" GridLinesVisibility="None"
                          CanUserResizeRows="False">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
                            <Setter Property="Padding" Value="15,10"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#EEEEEE"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border x:Name="CellBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="CellBorder" Property="Background" Value="#FFF5E8"/>
                                                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="#" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TransferID}" Foreground="#BDC3C7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="BRANCH" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ToBranchName}" FontSize="14"/>
                                        <StackPanel Orientation="Horizontal">

                                            <fa:ImageAwesome Icon="Calendar" Height="10" Foreground="#BDC3C7" Margin="0,0,5,0"/>

                                            <TextBlock Text="{Binding SentDate, StringFormat='{}{0:MMM dd, yyyy}'}" FontSize="11" Foreground="{StaticResource SecondaryText}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="ITEMS" Binding="{Binding TotalQuantity, StringFormat='N0'}" Width="80" ElementStyle="{StaticResource {x:Type TextBlock}}"/>

                        <DataGridTemplateColumn Header="STATUS" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#FEF5E7"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="In-Transit">
                                                        <Setter Property="Background" Value="#EBF5FB"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="In-Transit">
                                                            <Setter Property="Foreground" Value="#3498DB"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <Border x:Name="DetailsPanel" Grid.Column="2" Style="{StaticResource CardStyle}" RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <TransformGroup>
                    <TranslateTransform x:Name="PanelTranslate" X="20"/>
                    <ScaleTransform x:Name="PanelScale" ScaleX="1" ScaleY="1"/>
                </TransformGroup>
            </Border.RenderTransform>

            <Grid>
                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="REQUEST DETAILS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SecondaryText}"/>

                    <StackPanel Grid.Row="1" Margin="0,10,0,20">
                        <TextBlock x:Name="txtSelectedBranch" Text="Select a Request" FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryText}" TextWrapping="Wrap"/>
                        <Separator Background="#EEEEEE" Margin="0,15,0,0"/>
                    </StackPanel>

                    <ListView x:Name="lvDetails" Grid.Row="2" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border Background="Transparent" BorderBrush="#F5F7FA" BorderThickness="0,0,0,1" Padding="0,12">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Background="#F0F3F4" CornerRadius="5" Width="30" Height="30" Margin="0,0,12,0">
                                        <fa:ImageAwesome Icon="Cube" Foreground="#BDC3C7" Height="12" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ItemName}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryText}" FontSize="13"/>
                                        <TextBlock Text="Stock: Available" FontSize="10" Foreground="{StaticResource SuccessColor}"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding Quantity, StringFormat='x {0:N0}'}" FontWeight="Bold" Foreground="{StaticResource AccentColor}" VerticalAlignment="Center" FontSize="14"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,20">
                        <TextBlock Text="Total Items:" Foreground="{StaticResource SecondaryText}" Margin="0,0,10,0"/>
                        <TextBlock x:Name="txtTotalItems" Text="0" FontWeight="Bold" Foreground="{StaticResource PrimaryText}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4">
                        <StackPanel x:Name="ActionButtons">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="In-Transit">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <Button Click="Approve_Click" Style="{StaticResource ActionButtonStyle}" Background="{StaticResource SuccessColor}" BorderThickness="0" Margin="0,0,0,10">
                                <StackPanel Orientation="Horizontal">
                                    <fa:ImageAwesome Icon="Check" Foreground="White" Height="14" Margin="0,0,10,0"/>
                                    <TextBlock Text="APPROVE &amp; SHIP" FontWeight="Bold" Foreground="White" FontSize="14"/>
                                </StackPanel>
                            </Button>

                            <Button Click="Reject_Click" Style="{StaticResource ActionButtonStyle}" Background="Transparent" BorderThickness="1.5" BorderBrush="{StaticResource DangerColor}">
                                <TextBlock Text="REJECT REQUEST" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                            </Button>
                        </StackPanel>

                        <Border Background="#EBF5FB" BorderBrush="#3498DB" BorderThickness="1" CornerRadius="8" Padding="15">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="In-Transit">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <fa:ImageAwesome Icon="Truck" Foreground="#3498DB" Height="16" Margin="0,0,10,0"/>
                                <TextBlock Text="ITEM IS IN TRANSIT" FontWeight="Bold" Foreground="#3498DB"/>
                                <TextBlock Text="(Waiting for Receiver)" Foreground="{StaticResource SecondaryText}" Margin="5,0,0,0" FontSize="12" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid x:Name="StampOverlay" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="StampScale" ScaleX="2" ScaleY="2"/>
                            <RotateTransform Angle="-15"/>
                        </TransformGroup>
                    </Grid.RenderTransform>

                    <Border x:Name="StampBorder" BorderThickness="5" BorderBrush="{StaticResource SuccessColor}" CornerRadius="12" Padding="30,15" Background="#EEFFFFFF">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="Black" Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <fa:ImageAwesome x:Name="StampIcon" Icon="CheckCircle" Foreground="{StaticResource SuccessColor}" Height="50" Margin="0,0,0,10"/>
                            <TextBlock x:Name="StampText" Text="APPROVED" FontSize="32" FontWeight="Black" Foreground="{StaticResource SuccessColor}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>