<UserControl x:Class="SLICE_System.Views.FinanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             Background="#FDFBF7" FontFamily="Segoe UI">

    <UserControl.Resources>
        <DropShadowEffect x:Key="SoftShadow" BlurRadius="15" Color="#2C3E50" ShadowDepth="4" Opacity="0.15"/>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Profit &amp; Loss Ledger" FontSize="28" FontWeight="Black" Foreground="#2C3E50"/>
                <TextBlock Text="Consolidated Financial Performance &amp; Expense Tracking" FontSize="14" Foreground="#7F8C8D" Margin="0,2,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" Command="{Binding BackupDatabaseCommand}" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#2980B9" Foreground="White" FontWeight="Bold" Padding="15,10" BorderThickness="0" Cursor="Hand" ToolTip="Create a .bak file of the entire system">
                <StackPanel Orientation="Horizontal">
                    <fa:ImageAwesome Icon="Database" Height="14" Foreground="White" Margin="0,0,8,0"/>
                    <TextBlock Text="BACKUP DATABASE"/>
                </StackPanel>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#27AE60" CornerRadius="12" Padding="25,20" Margin="0,0,15,0" Effect="{StaticResource SoftShadow}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <fa:ImageAwesome Icon="LineChart" Height="14" Foreground="White" Opacity="0.8" Margin="0,0,8,0"/>
                        <TextBlock Text="TOTAL REVENUE" Foreground="White" Opacity="0.9" FontWeight="Bold" FontSize="12"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=₱ {0:N2}}" Foreground="White" FontWeight="Black" FontSize="32"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#E74C3C" CornerRadius="12" Padding="25,20" Margin="5,0,10,0" Effect="{StaticResource SoftShadow}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <fa:ImageAwesome Icon="ShoppingCart" Height="14" Foreground="White" Opacity="0.8" Margin="0,0,8,0"/>
                        <TextBlock Text="TOTAL EXPENSES" Foreground="White" Opacity="0.9" FontWeight="Bold" FontSize="12"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalExpenses, StringFormat=₱ {0:N2}}" Foreground="White" FontWeight="Black" FontSize="32"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#2980B9" CornerRadius="12" Padding="25,20" Margin="10,0,0,0" Effect="{StaticResource SoftShadow}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <fa:ImageAwesome Icon="Bank" Height="14" Foreground="White" Opacity="0.8" Margin="0,0,8,0"/>
                        <TextBlock Text="NET PROFIT" Foreground="White" Opacity="0.9" FontWeight="Bold" FontSize="12"/>
                    </StackPanel>
                    <TextBlock Text="{Binding NetProfit, StringFormat=₱ {0:N2}}" Foreground="White" FontWeight="Black" FontSize="32"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#FDEDEC" BorderBrush="#E74C3C" BorderThickness="1.5" CornerRadius="8" Padding="15" Margin="0,0,0,25">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <fa:ImageAwesome Grid.Column="0" Icon="ExclamationTriangle" Height="24" Foreground="#C0392B" Margin="0,0,15,0" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="COST OF WASTE &amp; SPOILAGE" Foreground="#C0392B" FontWeight="Black" FontSize="12"/>
                    <TextBlock Text="These losses are already deducted from your Net Profit via Total Expenses." Foreground="#E74C3C" FontSize="11" FontWeight="SemiBold" Margin="0,2,0,0"/>
                </StackPanel>

                <TextBlock Grid.Column="2" Text="{Binding TotalWasteCost, StringFormat=- ₱ {0:N2}}" Foreground="#C0392B" FontSize="20" FontWeight="Black" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="White" CornerRadius="12" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="#BDC3C7" ShadowDepth="4" Opacity="0.15"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding RecentTransactions}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="Transparent" BorderThickness="0" RowHeight="45">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F9F9"/>
                        <Setter Property="Foreground" Value="#95A5A6"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#EAECEE"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="#F4F6F7" BorderThickness="0,0,0,1">
                                        <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#F0F8FF"/>
                                <Setter Property="Foreground" Value="#2C3E50"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="DATE" Binding="{Binding TransactionDate, StringFormat='MMM dd, yyyy'}" Width="120" Foreground="#7F8C8D" FontWeight="SemiBold"/>

                    <DataGridTemplateColumn Header="TYPE" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F4F6F7" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Type}" FontWeight="Bold" FontSize="11" Foreground="#34495E"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="150" Foreground="#2C3E50" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="DESCRIPTION" Binding="{Binding Description}" Width="*" Foreground="#2C3E50"/>

                    <DataGridTextColumn Header="AMOUNT" Binding="{Binding Amount, StringFormat='₱ {0:N2}'}" Width="120" FontWeight="Black" Foreground="#2C3E50">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,15,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>