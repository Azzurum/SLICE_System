<UserControl x:Class="SLICE_System.Views.FinanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             Background="#FDFBF7">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Profit &amp; Loss Overview" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Consolidated Financial Performance" FontSize="14" Foreground="#7F8C8D"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#27AE60" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="TOTAL REVENUE" Foreground="White" Opacity="0.8" FontSize="12"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=₱ {0:N2}}" Foreground="White" FontWeight="Bold" FontSize="28"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#C0392B" CornerRadius="10" Padding="20" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="TOTAL EXPENSES" Foreground="White" Opacity="0.8" FontSize="12"/>
                    <TextBlock Text="{Binding TotalExpenses, StringFormat=₱ {0:N2}}" Foreground="White" FontWeight="Bold" FontSize="28"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#2980B9" CornerRadius="10" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="NET PROFIT" Foreground="White" Opacity="0.8" FontSize="12"/>
                    <TextBlock Text="{Binding NetProfit, StringFormat=₱ {0:N2}}" Foreground="White" FontWeight="Bold" FontSize="28"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#FDEDEC" BorderBrush="#E74C3C" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <fa:ImageAwesome Icon="ExclamationTriangle" Height="20" Foreground="#C0392B" Margin="0,0,15,0"/>
                <StackPanel>
                    <TextBlock Text="COST OF WASTE &amp; SPOILAGE (Included in Expenses)" Foreground="#C0392B" FontWeight="Bold" FontSize="12"/>
                    <TextBlock Text="{Binding TotalWasteCost, StringFormat=- ₱ {0:N2}}" Foreground="#C0392B" FontSize="18" FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="3" ItemsSource="{Binding RecentTransactions}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White" BorderBrush="#EEE" RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding TransactionDate, StringFormat='MMM dd, yyyy'}" Width="120"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}" Width="100" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>