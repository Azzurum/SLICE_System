<Window x:Class="SLICE_System.Views.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New User" Height="600" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Geometry x:Key="Vec_Times">M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z</Geometry>
        <Geometry x:Key="Vec_Save">M15,9H5V5H15M12,19A3,3 0 0,1 9,16A3,3 0 0,1 12,13A3,3 0 0,1 15,16A3,3 0 0,1 12,19M17,3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V7L17,3Z</Geometry>
    </Window.Resources>

    <Border Background="White" CornerRadius="16" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="Black" ShadowDepth="5" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="25,25,25,10">
                <StackPanel>
                    <TextBlock Text="New Account" FontSize="22" FontWeight="Black" Foreground="#2C3E50"/>
                    <TextBlock Text="Create credentials for a new employee." FontSize="12" Foreground="#95A5A6"/>
                </StackPanel>

                <Button Click="Cancel_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <Path Data="{StaticResource Vec_Times}" Fill="#95A5A6" Height="14" Width="14" Stretch="Uniform"/>
                </Button>
            </Grid>

            <StackPanel Grid.Row="1" Margin="25,10">

                <TextBlock Text="FULL NAME" FontSize="11" FontWeight="Bold" Foreground="#95A5A6" Margin="0,0,0,5"/>
                <TextBox x:Name="txtName" Tag="e.g. Juan Dela Cruz"/>

                <TextBlock Text="USERNAME" FontSize="11" FontWeight="Bold" Foreground="#95A5A6" Margin="0,15,0,5"/>
                <TextBox x:Name="txtUser" Tag="e.g. juan.delacruz"/>

                <TextBlock Text="PASSWORD" FontSize="11" FontWeight="Bold" Foreground="#95A5A6" Margin="0,15,0,5"/>
                <TextBox x:Name="txtPass" Tag="*******"/>

                <TextBlock Text="ROLE" FontSize="11" FontWeight="Bold" Foreground="#95A5A6" Margin="0,15,0,5"/>
                <ComboBox x:Name="cmbRole" Height="40" FontSize="14" Padding="10,8" Background="#F9F9F9" BorderBrush="#E0E0E0">
                    <ComboBoxItem Content="Clerk" IsSelected="True"/>
                    <ComboBoxItem Content="Manager"/>
                    <ComboBoxItem Content="Super-Admin"/>
                </ComboBox>

                <TextBlock Text="ASSIGNED BRANCH" FontSize="11" FontWeight="Bold" Foreground="#95A5A6" Margin="0,15,0,5"/>
                <ComboBox x:Name="cmbBranch" Height="40" FontSize="14" Padding="10,8" Background="#F9F9F9" BorderBrush="#E0E0E0"
                          DisplayMemberPath="BranchName" SelectedValuePath="BranchID"/>

                <TextBlock Text="* Super-Admins can access all branches regardless of selection." 
                           FontSize="10" Foreground="#BDC3C7" Margin="0,5,0,0" FontStyle="Italic"/>

            </StackPanel>

            <StackPanel Grid.Row="2" Margin="25" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="Cancel_Click" Width="100" Height="40" Background="Transparent" Foreground="#7F8C8D" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"/>

                <Button Name="btnSave" Click="Save_Click" Width="160" Height="40" Cursor="Hand" Margin="10,0,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#3498DB" CornerRadius="8">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Path Data="{StaticResource Vec_Save}" Fill="White" Height="14" Width="14" Margin="0,0,8,0" Stretch="Uniform"/>
                                    <TextBlock Text="CREATE USER" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>